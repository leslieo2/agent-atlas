# Public backend base URL used by the browser.
# Leave this unset in local development if you want the frontend to follow
# the current browser host (`localhost` or `127.0.0.1`) on port 8000.
# Change this when the API is not running on the default local port.
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000
